// Definición de un arreglo
let personas = ['Daniel', 'Mariana', 'Juan', 'Pedro'];
console.log(personas);

// Acceso a todos los elementos individuales del arreglo
for (let f = 0; f < personas.length; f++){
    console.log(personas[f]);
}