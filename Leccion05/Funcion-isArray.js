let arreglo = [1, 2, 3, 4, 5]; 

// Función para saber si un variable es un arrgelo o no
console.log(Array.isArray(arreglo));

// Como saber si una variable es un array mediante la funcion instanceof
console.log(arreglo instanceof Array); 
