// Definimos un nuevo objeto mediante el uso de "new"

// Creamos una referencia en memoria vacía, sin propiedades
let persona = new Object(); 
// Asignamos las propiedades del objeto
persona.cNombre = 'Daniel'; 
persona.cApellido = 'Alvarez'; 

// Accesamos las propiedades del objeto
console.log(persona.cNombre);

