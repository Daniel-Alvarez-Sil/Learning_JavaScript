// Operador Ternario
// Ternario = 3 partes
// Sintáxis:
    // (<operación boolean a evaluar>) ? <valor a retornar o comando a ejecutar en caso de true> : <valor a retornar o comando a ejecutar en caso de false>;

let resultado = (3 > 2) ? "TRUE" : "FALSE";
resultado; 

let iNum = 10; 
let parOImpar = iNum % 2 == 0 ? console.log("Valor par. ") : console.log("Valor impar. "); 