let iNum = 0, iMin = 0, iMax = 10;  

// Operadores Logicos
    // Operador AND, &&
    // Retorna TRUE solo si ambos operandos son verdaderos
    boolVar = iNum >= iMin && iNum <= iMax; 
    boolVar;
    // Operador OR, ||
    // Retorna TRUE si alguno de los dos operandos es verdadero
    boolVar = iNum == iMin || iNum == iMax; 
    boolVar;