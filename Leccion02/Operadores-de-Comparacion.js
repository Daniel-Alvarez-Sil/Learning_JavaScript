let a = 3, b = 2, c = "3";

// Operadores de Comparación
    // Igual a (sin importar el tipo)
    boolVar = a == c; 
    boolVar;
    // Igual a (valida primaremente los tipos)
    boolVar = a === c;
    boolVar;
    // Distinto a (sin importar el tipo)
    boolVar = a != c; 
    boolVar;
    // Distinto a (valida primeramente los tipos)
    boolVar = a !== c; 
    boolVar;